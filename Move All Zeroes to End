void pushZerosToEnd(vector<int>& arr) {
        // code here
        
       int count = 0; 
       for( int i = 0 ; i < arr.size() ; i++){
           if(arr[i]!=0){ 
               swap(arr[i], arr[count]); 
               count ++; 
           }
       }
    }
    1have a count
    2as you go thru the array if the array[i] is not zero, swap the arr at index i and arr at index count. for the numbers, the swap will return the same numbers.
    when array encounters 0, the count will not increase, therefore it will swap the 0 and the number 
